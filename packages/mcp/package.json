{
  "name": "@pascal-app/mcp",
  "version": "0.1.0",
  "description": "MCP server that exposes the Pascal 3D building editor as AI-native tools",
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "pascal-mcp": "./dist/index.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc --noEmit && esbuild src/index.ts --bundle --platform=node --format=esm --target=node18 --outfile=dist/index.js --banner:js=\"import { createRequire } from 'module'; const require = createRequire(import.meta.url);\"",
    "dev": "esbuild src/index.ts --bundle --platform=node --format=esm --target=node18 --outfile=dist/index.js --watch",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "@gltf-transform/core": "^4.1.1",
    "@pascal-app/core": "*",
    "zod": "^4.3.5"
  },
  "devDependencies": {
    "@pascal/typescript-config": "*",
    "@types/node": "^22.10.0",
    "esbuild": "^0.28.0",
    "typescript": "5.9.3"
  },
  "license": "MIT"
}
